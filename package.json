{
  "name": "swasthya-suchi",
  "version": "1.0.0",
  "description": "A centralized healthcare application",
  "main": "index.js",
  "scripts": {
    "start:client": "npm --prefix client start",
    "start:server": "npm --prefix server start",
    "start":"concurrently \"npm run start:client\" \"npm run start:server\"",
    "add:client":"npm install --prefix client",
    "add:server":"npm install --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
